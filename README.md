# Fitness-and-Health-prediction-system
ML Project

Run every file one by one.
First run the preprocessing file and save the cleaned dataset
Then, run the EDA file to gather insights
Then run the Data model file to prepare predictions and save the .pkl files for streamlit UI display
Finally, run app.py streamlit file using the command -- streamlit run app.py
